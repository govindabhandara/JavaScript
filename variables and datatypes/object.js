const student= {
    fullName:"rahul  kumar",
    age:24,
    cgpa:8.2,
    isPass:true
};

//to change the value in object
student["age"]=student["age"]+1;
student["fullName"]="govinda bhandara";

//we can't change the value in const but can change in the const object .